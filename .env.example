# MongoDB Connection
# Your MongoDB connection string for the voters database
MONGODB_URI=mongodb://your-mongodb-connection-string

# Optional: Specify the database name if different from default
# MONGODB_DB_NAME=voters
